I am creating a image generation app. I have all the backend modules done. I want to build a very modern, aesthtic looking, single page web ui that help me load/unload models, supply inputs (reference images, prompts), configure model parametes (num steps, guidance scale, resolution etc.). The UI should follow a simple linear vertical flow depicting the order of normal user interaction (i.e. select task (Text-to-image or image-to-text); Select model; Load/Unload model; Configure params; provide inputs; generate image; show generated image etc.). There should also be a settings ui which will help configure paths in 
